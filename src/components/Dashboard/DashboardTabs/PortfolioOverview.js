import React from 'react';
import './PortfolioOverview.css';

/**
 * Portfolio Overview tab — content removed per product request; tab remains in navigation.
 */
const PortfolioOverview = () => (
  <div className="portfolio-overview portfolio-overview--minimal" />
);

export default PortfolioOverview;
